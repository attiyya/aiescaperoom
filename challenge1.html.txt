<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Challenge 1: The Data Pipeline</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f0f4f8; /* Light blue-gray background */
        }
        .card {
            background-color: #ffffff;
            border-radius: 1rem; /* Rounded corners */
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
            max-width: 32rem; /* Constrain width for readability */
        }
        .btn {
            background-color: #4f46e5; /* Indigo 600 */
            color: white;
            padding: 0.75rem 1.5rem;
            border-radius: 0.5rem;
            font-weight: 600;
            transition: background-color 0.3s ease;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .btn:hover {
            background-color: #4338ca; /* Indigo 700 */
        }
        input[type="text"] {
            border: 1px solid #d1d5db; /* Gray 300 */
            padding: 0.75rem 1rem;
            border-radius: 0.5rem;
            width: 100%;
            box-sizing: border-box; /* Include padding in width */
            outline: none;
            transition: border-color 0.3s ease;
        }
        input[type="text"]:focus {
            border-color: #6366f1; /* Indigo 500 */
        }
        #messageBox {
            background-color: #e0f2fe; /* Light blue */
            border: 1px solid #90cdf4; /* Blue border */
            color: #2b6cb0; /* Dark blue text */
            padding: 0.75rem;
            border-radius: 0.5rem;
            margin-top: 1rem;
            display: none; /* Hidden by default */
            font-weight: 500;
        }
    </style>
</head>
<body class="flex items-center justify-center min-h-screen p-4">
    <div class="card p-8 space-y-6 text-center">
        <h1 class="text-3xl font-bold text-gray-800">Challenge 1: The Data Pipeline</h1>
        <p class="text-gray-600">The first step to ethical AI is understanding its raw materials. Unscramble the letters below to identify the essential data stream AI processes.</p>
        <div class="space-y-4">
            <p class="text-lg font-semibold text-gray-700">Unscramble the letters below:</p>
            <p class="text-4xl font-extrabold tracking-wider text-indigo-600 select-all">P U T I N</p>
            <input type="text" id="answerInput" placeholder="Enter your answer" class="text-center text-lg">
            <button class="btn" onclick="checkAnswer()">Submit</button>
            <p id="feedback" class="text-sm font-medium mt-2"></p>
            <div id="messageBox"></div>
        </div>
    </div>

    <script>
        // Function to show custom message box instead of alert
        function showMessageBox(message) {
            const msgBox = document.getElementById('messageBox');
            msgBox.textContent = message;
            msgBox.style.display = 'block';
            setTimeout(() => {
                msgBox.style.display = 'none';
            }, 3000); // Hide after 3 seconds
        }

        function checkAnswer() {
            const userAnswer = document.getElementById('answerInput').value.trim().toUpperCase();
            const correctAnswer = 'INPUT'; // P U T I N -> INPUT
            const feedbackElement = document.getElementById('feedback');

            if (userAnswer === correctAnswer) {
                feedbackElement.textContent = 'Success! You’ve identified the crucial first step. Your first piece of the final ethical code is: 1-[I].';
                feedbackElement.className = 'text-sm font-medium mt-2 text-green-600';
                document.getElementById('answerInput').disabled = true;
                document.querySelector('.btn').disabled = true;
                showMessageBox('Puzzle Solved! Great job on the data pipeline.');
            } else {
                feedbackElement.textContent = 'Incorrect. The data stream is still scrambled. Try again!';
                feedbackElement.className = 'text-sm font-medium mt-2 text-red-600';
                showMessageBox('Try again!');
            }
        }
    </script>
</body>
</html>
